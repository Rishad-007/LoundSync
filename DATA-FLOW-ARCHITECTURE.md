# LOUDSYNC Data Flow Architecture

## 📊 Complete System Data Flow

This document visualizes how data flows through the LOUDSYNC system from UI interactions to state updates and back to UI rendering.

---

## 🏗️ Layer Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                         UI LAYER                             │
│  React Native Components (Screens, Buttons, Lists)          │
│  - CreateSessionScreen                                       │
│  - JoinSessionScreen                                         │
│  - PlayerRoomScreen                                          │
└──────────────────┬──────────────────────────────────────────┘
                   │ dispatch actions
                   │ read state via hooks
                   ↓
┌─────────────────────────────────────────────────────────────┐
│                      HOOKS LAYER                             │
│  Custom Business Logic Hooks                                 │
│  - useSession()                                              │
│  - usePlayer()                                               │
│  - useDeviceList()                                           │
│  - useSyncMonitor()                                          │
│  - useHostControls()                                         │
└──────────────────┬──────────────────────────────────────────┘
                   │ read/write
                   ↓
┌─────────────────────────────────────────────────────────────┐
│                      STATE LAYER (Zustand)                   │
│  ┌─────────────┬──────────────┬────────────┬──────────────┐ │
│  │  userSlice  │ sessionSlice │ deviceSlice│ playbackSlice│ │
│  └─────────────┴──────────────┴────────────┴──────────────┘ │
│  ┌─────────────┬──────────────┐                             │
│  │  syncSlice  │ networkSlice │                             │
│  └─────────────┴──────────────┘                             │
│                                                              │
│  Single source of truth for all app state                   │
└──────────────────┬──────────────────────────────────────────┘
                   │ trigger effects
                   │ call methods
                   ↓
┌─────────────────────────────────────────────────────────────┐
│                    SERVICES LAYER                            │
│  Orchestration & Side Effects                                │
│  - SessionService (create/join)                              │
│  - SyncService (clock sync)                                  │
│  - AudioService (playback)                                   │
└──────────────────┬──────────────────────────────────────────┘
                   │ coordinate
                   ↓
┌─────────────────────────────────────────────────────────────┐
│                      CORE LAYER                              │
│  ┌─────────────┬──────────────┬────────────────────────┐    │
│  │   Network   │     Sync     │        Audio          │    │
│  │   Manager   │    Engine    │       Engine          │    │
│  │             │              │                        │    │
│  │ • WebSocket │ • ClockSync  │ • AudioBuffer         │    │
│  │ • Discovery │ • Timestamp  │ • AudioPlayer         │    │
│  │ • Messages  │ • Scheduler  │ • Decoder             │    │
│  └─────────────┴──────────────┴────────────────────────┘    │
│                                                              │
│  Pure logic, no state management, no UI dependencies        │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flow 1: Create Session (Host)

```
USER ACTION: Taps "Create Session"
      │
      ↓
┌─────────────────────────────────────────────────────────────┐
│ CreateSessionScreen.tsx                                      │
│   const { createSession } = useSessionActions();            │
│   await createSession('Party', 'track.mp3');                │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ sessionSlice.ts                                              │
│   createSession(name, trackUri) {                           │
│     1. Generate session ID                                   │
│     2. Get user from store                                   │
│     3. Create SessionInfo object                             │
│     4. Set mode = 'host', status = 'active'                 │
│     5. Call addDevice() for self                             │
│   }                                                          │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ deviceSlice.ts                                               │
│   addDevice(hostDevice) {                                    │
│     devices.set(hostDevice.id, hostDevice);                  │
│     setHostDevice(hostDevice);                               │
│   }                                                          │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ STATE UPDATED                                                │
│   sessionStore: { mode: 'host', sessionInfo: {...} }       │
│   deviceStore: { devices: [hostDevice], hostDevice }       │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ SERVICES LAYER (Would be called by service)                 │
│   SessionService.createSession()                             │
│     → NetworkManager.startServer(port)                       │
│     → DiscoveryService.startBroadcast(sessionInfo)           │
│     → AudioEngine.loadTrack(trackUri)                        │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ UI RE-RENDERS                                                │
│   useSessionMode() → 'host'                                  │
│   useSessionInfo() → { sessionName, hostIp, ... }           │
│   Navigate to /player-room                                   │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flow 2: Join Session (Client)

```
USER ACTION: Selects discovered session
      │
      ↓
┌─────────────────────────────────────────────────────────────┐
│ JoinSessionScreen.tsx                                        │
│   const { joinSession } = useSessionActions();              │
│   const { performSync } = useClientSync();                  │
│   await joinSession(hostIp, sessionId);                     │
│   await performSync();                                       │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ sessionSlice.ts                                              │
│   joinSession(hostIp, sessionId) {                          │
│     1. Get user from store                                   │
│     2. Create preliminary SessionInfo                        │
│     3. Set mode = 'client', status = 'active'               │
│   }                                                          │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ SERVICES LAYER                                               │
│   SessionService.joinSession(hostIp)                         │
│     → NetworkManager.connect(hostIp:8080)                    │
│     → Send JOIN_REQUEST                                      │
│     → Receive SESSION_INFO from host                         │
│     → Update sessionStore.sessionInfo                        │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ SyncService.performSync()                                    │
│   1. ClockSync.syncWithHost()                                │
│      t0 = now()                                              │
│      send SYNC_REQUEST(t0)                                   │
│      receive SYNC_RESPONSE(t1, t2)                           │
│      t3 = now()                                              │
│      calculate:                                              │
│        latency = ((t3-t0) - (t2-t1)) / 2                    │
│        offset = ((t1-t0) + (t2-t3)) / 2                     │
│   2. Update syncStore                                        │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ syncSlice.ts                                                 │
│   updateSyncMetrics({                                        │
│     clockOffset: 45,                                         │
│     latency: 12,                                             │
│     jitter: 3                                                │
│   })                                                         │
│   → Calculate quality based on metrics                       │
│   → Set status = 'synced'                                    │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ STATE UPDATED                                                │
│   sessionStore: { mode: 'client', sessionInfo: {...} }     │
│   syncStore: { status: 'synced', metrics: {...} }          │
│   networkStore: { connectionStatus: 'connected' }           │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ UI RE-RENDERS                                                │
│   useSessionMode() → 'client'                                │
│   useIsSynced() → true                                       │
│   useSyncMonitor() → { offsetMs: 45, latencyMs: 12 }       │
│   Navigate to /player-room                                   │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flow 3: Synchronized Playback

```
USER ACTION: Host presses Play
      │
      ↓
┌─────────────────────────────────────────────────────────────┐
│ PlayerRoomScreen.tsx (Host)                                  │
│   const { broadcastPlay } = useHostControls();              │
│   const scheduledTime = Date.now() + 500; // buffer         │
│   broadcastPlay(scheduledTime);                             │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ playbackSlice.ts                                             │
│   play(scheduledTime) {                                      │
│     playback.state = 'playing'                               │
│     playback.scheduledPlayTime = scheduledTime               │
│   }                                                          │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ SERVICES LAYER                                               │
│   AudioService.syncedPlay(scheduledTime)                     │
│     → AudioEngine.playAt(scheduledTime)                      │
│   NetworkService.broadcast({                                 │
│     type: 'PLAY',                                            │
│     timestamp: scheduledTime,                                │
│     position: 0                                              │
│   })                                                         │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ├─────────────────────────────────┐
                   │                                 │
                   ↓ (to all clients)                ↓ (local)
┌──────────────────────────────────┐  ┌──────────────────────┐
│ CLIENT DEVICES                   │  │ HOST DEVICE          │
│ NetworkManager.onMessage('PLAY') │  │ AudioEngine.playAt() │
│   → Extract: timestamp, position │  │   Schedules play at  │
│   → Get clockOffset from store   │  │   exact timestamp    │
│   → adjustedTime =               │  └──────────────────────┘
│       timestamp + clockOffset    │
│   → AudioEngine.playAt(adjusted) │
└──────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ ALL DEVICES                                                  │
│   AudioEngine plays at synchronized timestamp                │
│   Accuracy: ±30ms (excellent sync)                           │
│                                                              │
│   Host plays at:   t = 1000                                  │
│   Client 1 plays:  t = 1000 + 45ms = 1045                   │
│   Client 2 plays:  t = 1000 - 12ms = 988                    │
│   Client 3 plays:  t = 1000 + 8ms  = 1008                   │
│                                                              │
│   Result: Perceptually synchronized audio                    │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ PLAYBACK UPDATES                                             │
│   AudioEngine.onPlaybackUpdate((state) => {                 │
│     playbackStore.updatePosition(state.position)             │
│     playbackStore.setPlaybackState(state.state)              │
│   })                                                         │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ UI UPDATES (All devices)                                     │
│   useIsPlaying() → true                                      │
│   usePlayback() → { state: 'playing', position: ... }       │
│   Progress bar updates in real-time                          │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flow 4: Device Discovery

```
CLIENT starts scanning
      │
      ↓
┌─────────────────────────────────────────────────────────────┐
│ JoinSessionScreen.tsx                                        │
│   const { startDiscovery } = useSessionDiscovery();         │
│   startDiscovery();                                          │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ networkSlice.ts                                              │
│   setDiscovering(true)                                       │
│   clearDiscoveredSessions()                                  │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ SERVICES LAYER                                               │
│   DiscoveryService.startListening()                          │
│     → Listen on UDP port 4545                                │
│     → Wait for SESSION_ANNOUNCE packets                      │
└──────────────────────────────────────────────────────────────┘
                   ↑
                   │ UDP broadcast
                   │
┌─────────────────────────────────────────────────────────────┐
│ HOST SIDE                                                    │
│   DiscoveryService.startBroadcast(sessionInfo)               │
│     Every 2 seconds:                                         │
│       → UDP broadcast on 255.255.255.255:4545                │
│       → Payload: {                                           │
│           type: 'SESSION_ANNOUNCE',                          │
│           sessionId, sessionName, hostIp, port               │
│         }                                                    │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓ (received by clients)
┌─────────────────────────────────────────────────────────────┐
│ CLIENT SIDE                                                  │
│   DiscoveryService.onPacketReceived((packet) => {           │
│     if (packet.type === 'SESSION_ANNOUNCE') {               │
│       networkStore.addDiscoveredSession(packet)              │
│     }                                                        │
│   })                                                         │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ networkSlice.ts                                              │
│   addDiscoveredSession(session) {                           │
│     if (exists) update else add                              │
│     discoveredSessions = [...sessions, session]              │
│   }                                                          │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ UI RE-RENDERS                                                │
│   useDiscoveredSessions() → [session1, session2, ...]      │
│   FlatList updates with available sessions                   │
│   User can tap to join                                       │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flow 5: Position Updates (During Playback)

```
AudioEngine playing
      │
      ↓
┌─────────────────────────────────────────────────────────────┐
│ CORE: AudioEngine                                            │
│   setInterval(() => {                                        │
│     currentPosition = getCurrentTime()                       │
│     emit('positionUpdate', currentPosition)                  │
│   }, 100) // every 100ms                                     │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ SERVICES: AudioService                                       │
│   audioEngine.on('positionUpdate', (position) => {          │
│     playbackStore.updatePosition(position)                   │
│   })                                                         │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ playbackSlice.ts                                             │
│   updatePosition(position) {                                 │
│     playback.position = position                             │
│   }                                                          │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────────┐
│ UI: PlayerRoomScreen                                         │
│   const { position, duration } = usePlayback();             │
│   const progress = (position / duration) * 100;             │
│                                                              │
│   <Slider value={progress} />                                │
│   <Text>{formatTime(position)} / {formatTime(duration)}</Text>│
└──────────────────────────────────────────────────────────────┘
```

---

## 📊 State Update Frequency

| Event               | Frequency     | Impact            |
| ------------------- | ------------- | ----------------- |
| Position updates    | 100ms (10 Hz) | Progress bar      |
| Clock sync          | 30s           | Offset correction |
| Network metrics     | 5s            | Quality indicator |
| Device heartbeat    | 10s           | Connection status |
| Discovery broadcast | 2s            | Session list      |
| User interactions   | On demand     | Immediate         |

---

## 🎯 Key Principles

### 1. **Unidirectional Data Flow**

```
UI → Actions → State → UI
```

Never: UI → State directly
Never: State → UI directly (always via hooks)

### 2. **Single Source of Truth**

```
All app state lives in Zustand store
Services/Core layers are stateless
They read from and write to store only
```

### 3. **Separation of Concerns**

```
UI: Presentation only
Hooks: Business logic + derived state
Store: State + actions
Services: Orchestration
Core: Pure algorithms
```

### 4. **Selective Re-renders**

```
Use specific selectors:
  useSessionMode()      ✓ Only re-renders when mode changes
  useLoudSyncStore()    ✗ Re-renders on ANY change

Use custom hooks:
  usePlayer()           ✓ Combines related state
  Multiple direct calls ✗ More verbose, more re-renders
```

### 5. **Type Safety**

```
Every state slice is fully typed
Every action has proper signature
Every selector returns typed value
No 'any' types allowed
```

---

## 🚀 Performance Optimizations

### 1. **Map for Devices**

```typescript
devices: Map<string, Device>  // O(1) lookup
vs
devices: Device[]              // O(n) lookup
```

### 2. **Selective Subscriptions**

```typescript
// Good: Only subscribes to playback.state
const state = useLoudSyncStore((s) => s.playback.state);

// Bad: Subscribes to everything
const { playback } = useLoudSyncStore();
```

### 3. **Memoized Helpers**

```typescript
const { devices } = useDeviceList(); // Memoized in hook
vs;
const devices = Array.from(store.devices.values()); // New array every call
```

### 4. **Batch Updates**

```typescript
// Good: Single state update
set({
  mode: "host",
  status: "active",
  sessionInfo: info,
});

// Bad: Multiple re-renders
set({ mode: "host" });
set({ status: "active" });
set({ sessionInfo: info });
```

---

## 📝 Summary

The LOUDSYNC state architecture provides:

✅ **Clear data flow** from UI → State → Services → Core  
✅ **Type safety** with full TypeScript coverage  
✅ **Performance** via selective subscriptions and memoization  
✅ **Maintainability** through separation of concerns  
✅ **Scalability** with modular slice architecture  
✅ **Developer experience** with custom hooks and helpers

**Next step**: Implement the Services layer that connects State to Core modules.
